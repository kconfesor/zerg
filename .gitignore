# build output
/zerg
/dist/
web/dist/
web/node_modules/

# runtime state
.zerg/
.worktrees/

# os
.DS_Store

# internal/api/dist is deliberately NOT ignored. //go:embed requires the
# directory to exist at compile time, so ignoring it would break `go build`,
# `go test` and `go vet` on a fresh clone. The built cockpit is committed;
# ./build.sh regenerates it.
.pi/todos
