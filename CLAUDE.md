The rules for working in this repository are harness-neutral and live in one place, so that an
agent on any harness is held to the same ones.

@AGENTS.md

`.claude/commands/` adds runnable versions of the loops described there: `/verify`, `/dev` and
`/migration`. The `add-feature` skill covers a change that crosses the daemon and the cockpit.
